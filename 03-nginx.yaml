- name: Install and run nginx
  become: yes
  hosts: web
  tasks:
    - name: Task 01 Install nginx
      ansible.builtin.package:
        name: nginx
        state: present

    - name: Task 02 Start and Enable nginx
      ansible.builtin.package:
        name: nginx
        enabled: true